function SearchPage() {
  return (
    <main
      aria-label="Search page"
      style={{
        minHeight: '100vh',
        width: '100%',
        backgroundColor: '#fff',
      }}
    />
  );
}

export default SearchPage;
